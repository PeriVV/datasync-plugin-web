# 数据同步工具用户使用手册

## 1. 工具用途

数据同步工具用于在不同数据库环境之间迁移、恢复和核对业务任务数据。

用户可以通过页面完成以下工作：

- 维护数据库连接
- 查看数据库表和样例数据
- 配置任务数据导出的表范围
- 生成离线数据包
- 将离线数据包导入到目标库
- 查看比对、同步、备份等功能入口

本手册面向普通使用人员，重点说明页面如何操作、参数如何填写、结果如何判断。

## 2. 进入系统

打开浏览器，访问数据同步工具页面。

进入后会看到“工作中心”页面。工作中心提供常用功能入口，包括：

- 新增连接
- 生成离线包
- 导入数据包
- 执行比对
- 执行同步
- 任务备份
- 数据库模板配置

左侧菜单是主要操作入口。日常使用通常按以下顺序进行：

1. 配置数据源。
2. 确认业务数据模型和导出模板。
3. 执行数据导出。
4. 执行数据导入。
5. 根据需要做比对、同步或备份。

## 3. 常用业务流程

### 3.1 从一个环境导出任务数据

适用场景：

- 从中心库导出某个任务的数据。
- 把某个业务任务打包成离线包。
- 将任务数据交付给其他环境导入。

操作步骤：

1. 进入“数据源连接”，确认源数据库连接正常。
2. 进入“数据导出”。
3. 选择要导出的数据源。
4. 选择业务数据模型或导出模板。
5. 填写任务标识。
6. 如需导出附件，填写附件根目录并打开“导出本地附件”。
7. 填写导出包文件名。
8. 点击“生成导出包”。
9. 根据浏览器提示保存 zip 文件。

导出成功后，页面会提示导出包已保存，或提示浏览器已经开始下载。

### 3.2 把离线包导入目标环境

适用场景：

- 把导出的 zip 数据包恢复到目标数据库。
- 将任务数据从一个环境迁移到另一个环境。
- 验证离线包是否可以正常落库。

操作步骤：

1. 进入“数据源连接”，确认目标数据库连接正常。
2. 进入“数据导入”。
3. 选择离线包 zip 文件。
4. 选择目标数据库。
5. 选择或填写目标文件根目录。
6. 选择失败处理方式。
7. 点击“执行导入”。

导入请求执行完成后，页面会显示操作提示。执行失败时，应先根据错误提示排查原因，再决定是否重新导入。

### 3.3 维护导出范围

适用场景：

- 某个项目新增了业务表。
- 某张表的任务筛选条件变化。
- 附件表字段或附件路径规则变化。
- 需要新增或调整备份表映射。

操作步骤：

1. 进入“数据库模板配置”。
2. 在左侧选择业务数据模型。
3. 按向导确认基本信息、主表、关联表和附件。
4. 输入一个真实业务标识测试模型。
5. 点击“保存模型”。

复杂历史规则会以只读高级规则显示，不建议直接修改底层 JSON。

### 3.4 使用固定导出模板

适用场景：

- 经常导出同一个任务。
- 经常按同一套规则执行全库导出。
- 希望减少每次导出时手工填写参数。

操作步骤：

1. 进入“策略模板配置”。
2. 在左侧选择已有模板。
3. 确认模板名称、导出范围、适用模型、附件、失败处理、压缩和日志配置。
4. 如需修改，调整后点击“保存模板”。
5. 进入“数据导出”页面，在“导出模板”中选择该模板。

选择导出模板后，页面会自动应用模板中的导出范围、适用模型、附件和处理方式。

## 4. 页面说明

## 4.1 工作中心

工作中心是系统首页，主要用于快速进入常用功能。

页面包含：

- 快捷入口：点击后跳转到对应功能。
- 历史处理任务：展示示例或最近任务信息。
- 接入提醒：展示当前工具能力和注意事项。

如果不确定从哪里开始，优先点击“新增连接”或“生成离线包”。

## 4.2 数据源连接

数据源连接用于维护数据库连接。导出、导入等操作都依赖这里配置的数据源。

### 新增连接

点击右上角“新增”，填写连接信息。

字段说明：

- 名称：给连接起一个容易识别的名字，例如“中心库”“节点库”“导入目标库”。
- 数据库类型：选择 MySQL 或 DM8。
- 主机名：数据库服务器地址。
- 端口：数据库端口，MySQL 通常是 3306，DM8 通常是 5236。
- 数据库名 / Schema：MySQL 填数据库名，DM8 填 Schema。
- 用户名：数据库登录账号。
- 密码：数据库登录密码。
- 驱动类：一般不用填写，特殊环境由管理员提供。
- 连接 URL：可以点击“生成URL”自动生成，也可以手工填写。

填写完成后，可以先点击“测试连接”。测试成功后，再点击“保存并连接”。

### 查看连接状态

左侧会列出已有连接。点击某个连接后，右侧显示连接详情。

连接状态包括：

- 正常：连接可用。
- 异常：连接失败或配置有误。
- 未检测：尚未刷新状态。
- 已禁用：连接已停用。

点击“测试连通”可以重新检查当前连接。

### 查看数据表

选择连接后，下方“数据表预览”会展示该数据库中的表。

可以：

- 搜索表名。
- 点击表名查看样例数据。
- 点击“刷新表”重新读取表列表。

这一步常用于确认是否选对数据库。

## 4.3 数据库模板配置

业务数据模型告诉系统“一项业务数据包括哪些表、哪些附件，以及如何通过业务标识查出完整数据”。

页面采用五步向导，用户不需要直接编写 SQL 查询条件。

### 数据库模型列表

左侧显示已有数据库模型文件。点击文件名后，右侧显示详情。

可用操作：

- JSON：查看完整底层配置。
- 删除：删除该数据库模型文件。
- 刷新：重新读取数据库模型列表。

删除操作不可随意执行，删除后相关导出模板可能无法使用。

### 第一步：基本信息

- 模板名称：面向业务人员的名称，例如“任务数据模型”。
- 适用数据源：模型对应的数据库连接。
- 说明：描述模型用于哪些导出、导入、同步或备份场景。

### 第二步：设置查找条件

- 主表：业务数据的入口表，例如 `DS_TASK`。
- 主键字段：唯一标识主表记录的字段，例如 `ID`。
- 业务标识名称：页面展示给使用者的名称，例如“任务ID”。

### 第三步：配置关联表

添加关联表时，可以搜索数据库表并分页多选。为每张关联表选择关联到主表的字段，例如选择 `DS_TASK_DETAIL.TASK_ID` 关联到 `DS_TASK.ID` 后，系统会自动生成查询规则。关联表较多时，配置区域也会分页展示。

已有复杂规则会以只读高级规则显示，页面不会自动改写。

### 第四步：配置附件

- 附件表：通过下拉框选择记录附件信息的业务表，可选择一到两张或按实际情况多选。
- 文件路径字段：保存附件相对路径的字段。
- 文件名字段：保存文件名的字段。
- 随数据包导出：控制附件是否随离线包导出。

备份表映射位于本步骤的高级设置中，会自动沿用关联表查询规则。

### 第五步：测试模型

输入一个真实业务标识后，系统会只读查询数据库，并预览涉及表、记录总数和附件总数。

## 4.4 策略模板配置

业务数据模型解决“导哪些表、表之间怎么关联”；导出模板解决“按什么方式导出”。选择模板后，导出页面会自动应用相关配置。

### 模板列表

左侧显示已有导出模板。点击模板名称后，右侧显示详情。

可用操作：

- JSON：查看模板 JSON。
- 删除：删除模板文件。
- 刷新：重新加载模板列表。

### 模板详情

字段说明：

- 模板名称：给用户看的名称。
- 导出范围：选择“按任务ID导出”或“全库导出”。
- 适用模型：按任务ID导出时选择业务数据模型；全库导出不需要模型。
- 是否包含附件：控制是否复制模型中配置的附件。
- 失败处理方式：遇到失败立即停止，或跳过失败继续导出。
- 压缩格式：当前支持 zip。
- 是否生成日志：控制离线包内是否生成导出日志。

导出模式说明：

- 按任务ID导出：只导出指定任务相关数据，日常最常用。
- 全库导出：导出当前数据库范围内的数据，数据量可能较大，使用前应确认用途。

## 4.5 数据导出

数据导出用于生成 zip 离线包。

字段说明：

- 数据源：选择从哪个数据库导出。
- 导出模板：选择预设模板；如果没有合适模板，选择“不使用导出模板”。
- 业务数据模型：选择本次按任务ID导出使用的业务数据模型。
- 任务标识：要导出的任务 ID。支持多个，多个任务可用换行、逗号或空格分隔。
- 本地附件根目录：附件文件所在根目录。
- 导出包文件名：最终生成的 zip 文件名。
- 启用自动任务范围：通常保持开启。
- 导出本地附件：需要连同附件一起迁移时开启。

推荐操作：

1. 先选择数据源。
2. 再选择导出模板。
3. 检查任务标识是否正确。
4. 确认附件根目录是否正确。
5. 点击“生成导出包”。

导出成功后，请保存浏览器下载的 zip 文件。这个 zip 文件就是后续导入使用的离线包。

注意事项：

- 任务标识必须准确，否则可能导出不到数据或导出错误任务的数据。
- 开启附件导出时，附件根目录必须正确。
- 全库导出可能耗时较长，也可能生成较大的 zip 文件。

## 4.6 数据导入

数据导入用于把 zip 离线包恢复到目标数据库。

字段说明：

- 离线包路径：选择之前导出的 zip 文件。
- 目标数据库：选择要写入数据的数据库。
- 目标文件根目录：附件文件恢复到哪个目录。
- 失败处理方式：遇到失败立即停止，或跳过失败数据继续导入。

推荐操作：

1. 先确认目标数据库连接正常。
2. 选择离线包。
3. 选择目标数据库。
4. 设置目标文件根目录。
5. 首次导入建议选择“遇到失败立即停止”。
6. 点击“执行导入”。

导入前建议确认目标库是否需要备份，避免误写入。

## 4.7 差异比对

差异比对用于检查两个环境中同一任务范围的数据是否一致。

页面字段：

- 比对模式：选择增量比对或全量比对。
- 任务标识：要检查的任务 ID。
- 源库 / 目标库：选择以哪个库为标准，检查哪个库。

使用建议：

- 同步或导入前，可以先执行一次比对，确认差异范围。
- 同步或导入后，再执行一次比对，确认数据是否收敛。

当前说明：

该页面已经提供操作入口，但是否可真实执行取决于后端是否开放比对能力。如果点击后提示接口不支持，请联系维护人员确认功能是否已启用。

## 4.8 在线同步

在线同步用于把源库任务数据直接写入目标库。

页面字段：

- 同步模式：选择增量同步或全量同步。
- 任务标识：要同步的任务 ID。
- 附件同步：是否同步附件文件。
- 中心库：同步来源。
- 节点库：同步目标。

使用建议：

- 在线同步会写入目标库，执行前应先做差异比对。
- 附件同步开启前，应确认附件路径配置正确。
- 全量同步影响范围较大，应谨慎使用。

当前说明：

该页面已经提供操作入口，但是否可真实执行取决于后端是否开放同步能力。如果点击后提示接口不支持，请联系维护人员确认功能是否已启用。

## 4.9 任务备份

任务备份用于把指定任务的数据复制到备份表。

页面字段：

- 任务标识：要备份的任务 ID。
- 源环境：从哪个数据库读取数据。
- 数据库模型：选择包含备份表映射的数据库模型。

使用建议：

- 任务备份只处理数据库记录，不处理附件文件。
- 执行前应确认数据库模型里已经配置备份表映射。
- 备份完成后，页面会显示操作成功或失败提示。

当前说明：

该页面已经提供操作入口，但是否可真实执行取决于后端是否开放备份能力。如果点击后提示接口不支持，请联系维护人员确认功能是否已启用。

## 5. 字段填写说明

### 任务标识

任务标识通常是业务任务 ID。导出、导入后的核对、比对、同步、备份都围绕它进行。

填写要求：

- 必须与业务库中的任务 ID 一致。
- 多个任务可以换行填写。
- 也可以用逗号、空格分隔。

### 数据库模型

数据库模型是“导出哪些表、如何按任务筛选”的规则。

如果不确定选哪一个，通常选择系统默认提供的业务数据库模型。

### 导出模板

导出模板是预设的一套导出行为参数，不负责定义业务表关系。

如果已经有适合当前任务的模板，优先选择模板；如果没有，选择“不使用导出模板”并手动填写页面参数。

### 附件根目录

附件根目录是附件文件所在的最上层目录。

导出时，它表示从哪里读取附件。

导入时，它表示把附件恢复到哪里。

填写前应确认：

- 路径存在。
- 当前服务有读取或写入权限。
- 路径与业务系统实际附件存储位置一致。

### 失败处理方式

失败处理方式用于控制导入时遇到 SQL 错误后的处理方式。

- 遇到失败立即停止：适合首次验证和正式导入。
- 跳过失败数据，继续导入：适合已明确知道部分错误可以忽略的场景。

## 6. 操作提示

导入、导出和备份执行完成后，页面会显示成功或失败提示。

判断原则：

- 页面显示成功提示，通常表示操作已经执行完成。
- 页面显示失败提示时，需要根据提示内容排查原因。
- 导出完成后，应确认 zip 文件已成功下载。
- 导入完成后，应在目标库或业务系统中抽查关键数据。

## 7. 操作注意事项

- 导出前确认选中的是正确源库。
- 导入前确认选中的是正确目标库。
- 导入会写入目标数据库，正式操作前建议先备份目标库。
- 全库导出、全量同步影响范围较大，使用前应确认业务需求。
- 修改数据库模型会影响后续导出、同步、比对和备份。
- 删除业务数据模型或导出模板前，应确认没有其他人正在使用。
- 附件相关操作必须确认路径正确。
- 首次执行导入建议选择“遇到失败立即停止”。

## 8. 常见问题

### 8.1 页面里没有可选数据源

可能原因：

- 尚未新增数据库连接。
- 数据源保存失败。
- 当前用户环境无法访问后端服务。

处理方式：

1. 进入“数据源连接”。
2. 点击“新增”。
3. 填写数据库信息。
4. 测试连接成功后保存。

### 8.2 测试连接失败

可能原因：

- 主机名或端口错误。
- 数据库服务未启动。
- 用户名或密码错误。
- 当前网络无法访问数据库。
- 数据库账号权限不足。

处理方式：

1. 检查数据库地址。
2. 检查账号密码。
3. 联系数据库管理员确认网络和权限。

### 8.3 导出时提示任务标识不能为空

按任务导出时必须填写任务标识。

处理方式：

- 在“任务标识”中填写正确任务 ID。
- 如果选择的是全库导出模板，确认模板是否正确加载。

### 8.4 导出后没有附件

可能原因：

- 未开启“导出本地附件”。
- 附件根目录填写错误。
- 数据库模型中文件表、文件列或文件路径模板配置不正确。
- 当前任务没有附件。

处理方式：

1. 确认“导出本地附件”已开启。
2. 检查附件根目录。
3. 检查数据库模型中的附件配置。

### 8.5 导入失败

可能原因：

- 选择的 zip 不是本工具导出的离线包。
- 目标数据库连接异常。
- 目标数据库账号没有写入权限。
- SQL 执行报错。
- 附件目标目录不可写。

处理方式：

1. 确认离线包来源。
2. 测试目标数据库连接。
3. 查看页面错误信息。
4. 必要时联系维护人员查看导入日志。

### 8.6 比对、同步或备份提示接口不支持

说明当前环境可能尚未启用对应后端能力。

处理方式：

- 联系维护人员确认该功能是否已上线。
- 如果只是演示页面，可使用模拟数据环境。

## 9. 推荐使用规范

- 每次正式导入前，先确认目标库和任务 ID。
- 重要环境导入前先备份。
- 对同一任务，建议保留导出包文件，便于追溯。
- 修改业务数据模型和导出模板前，先复制 JSON 备份。
- 导出完成后，抽查离线包是否包含 SQL 和附件。
- 导入完成后，抽查业务系统页面或数据库记录。
- 不确定字段含义时，不要随意修改业务数据模型和导出模板。

## 10. 简短操作清单

导出任务数据：

1. 数据源连接，确认源库正常。
2. 数据导出，选择源库。
3. 选择业务数据模型或导出模板。
4. 填写任务标识。
5. 确认附件根目录。
6. 生成导出包。
7. 保存 zip 文件。

导入任务数据：

1. 数据源连接，确认目标库正常。
2. 数据导入，选择 zip 离线包。
3. 选择目标数据库。
4. 设置目标文件根目录。
5. 选择“遇到失败立即停止”。
6. 执行导入。
7. 确认页面显示导入成功提示。
